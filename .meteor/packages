# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.3.0             # Packages every Meteor app needs to have
mobile-experience@1.0.5       # Packages for a great mobile UX
mongo@1.4.2                   # The database Meteor supports right now
blaze-html-templates@1.0.4    # Compile .html files into Meteor Blaze views
reactive-var@1.0.11           # Reactive variable for tracker
tracker@1.1.3                 # Meteor's client-side reactive programming library

standard-minifier-css@1.4.0   # CSS minifier run for production mode
standard-minifier-js@2.3.1    # JS minifier run for production mode
es5-shim@4.7.0                # ECMAScript 5 compatibility for older browsers
ecmascript@0.10.0             # Enable ECMAScript2015+ syntax in app code
shell-server@0.3.1            # Server-side component of the `meteor shell` command



fortawesome:fontawesome       # Scalable vector icons hich are customizable via CSS (Retina Friendly)
twbs:bootstrap
iron:router                   # A router that works on the server and the browser, designed specifically for Meteor
themeteorchef:bert            # A client side, multi-style alerts system for Meteor
accounts-base                 # Implements the basic functions necessary for user accounts 
accounts-password             # A login service that enables secure password-based login
session                       # Session provides a global object on the client that you can use to store an arbitrary set of key-value pairs. 
                              # (cont->) Use it to store things like the currently selected item in a list.
                              
meteortoys:allthings          # An in-app debugOnly development tool
cfs:standard-packages         #  
cfs:gridfs                    #  
momentjs:moment
copleykj:livestamp
wizonesolutions:underscore-string
alanning:roles
